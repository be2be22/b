[build]
builder = "nixpacks"
buildCommand = "pip install fastapi 'uvicorn[standard]' psutil httpx"

[deploy]
startCommand = "sh -c 'uvicorn main:app --host 0.0.0.0 --port $PORT --no-access-log'"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
